# 心音拍点检测算法重新设计

## 概述

重新设计了心音音频拍点检测算法，提供更智能的 S1/S2 识别和可配置的心率范围支持。

## 核心功能

### 1. S1/S2 智能识别

提供三种 S1/S2 处理模式：

#### 🤖 自动模式 (Auto)
- **特点**：智能识别并处理 S1/S2 配对
- **适用场景**：大多数情况的首选，能够自适应处理：
  - 完整的 S1-S2 配对
  - 部分拍点缺失 S2 的情况
  - S2 强度变化的情况
- **工作原理**：
  - 自动识别 S1-S2 时间间隔特征（systole: 20-180ms）
  - 基于强度比较区分 S1（通常较强）和 S2（通常较弱）
  - S2 给予低权重但不完全排除
  - 对缺失 S2 的拍点也能正确识别 S1
  - 使用自适应阈值和间隔验证

#### 🎯 仅 S1 模式 (S1-Only)
- **特点**：只检测 S1 心音，完全忽略 S2
- **适用场景**：
  - S2 不明显或无 S2 的心音
  - 需要最简单、最快速的检测
  - S1 特征清晰的情况
- **工作原理**：
  - 过滤掉所有识别为 S2 的声音
  - 只选择强度较高的 onset 作为 S1
  - 使用基于强度的自适应阈值

#### 👥 S1-S2 配对模式 (S1-S2-Pair)
- **特点**：要求 S1-S2 必须成对出现
- **适用场景**：
  - S1 和 S2 都很清晰的心音
  - 需要最高准确度
  - 愿意牺牲部分拍点来保证质量
- **工作原理**：
  - 严格要求每个拍点必须有配对的 S2
  - 跳过没有配对的 onset
  - 使用 S1 作为拍点标记

### 2. 可配置心率范围

#### 支持的心率范围
- **最小心率**：30-150 BPM（默认 40 BPM）
- **最大心率**：100-350 BPM（默认 300 BPM）
- **用户可调**：通过 UI 滑块调整

#### 为什么需要配置心率范围？
1. **提高准确度**：缩小范围可以减少误判
2. **适应不同场景**：
   - 成人静息：50-100 BPM
   - 运动/儿童：100-200 BPM
   - 新生儿：120-160 BPM
   - 运动员：40-60 BPM
3. **减少误检测**：排除不可能的心率值

#### 使用建议
- **已知大致心率**：设置接近实际值的范围（如 60-120 BPM）
- **心率变化大**：使用较宽范围（如 40-200 BPM）
- **不确定**：使用默认值（40-300 BPM）

## 技术实现

### S1/S2 识别算法

```typescript
interface OnsetInfo {
  time: number;         // onset 时间
  strength: number;     // onset 强度
  isS2: boolean;        // 标记为 S2
  isPaired: boolean;    // 是否为配对的一部分
  pairIndex: number;    // 配对伙伴的索引
}
```

#### 配对识别逻辑

1. **时间特征识别**
   - S1-S2 间隔（systole）：20-180ms
   - S2-S1 间隔（diastole）：≥150ms
   - 最小区分间隔：20ms

2. **强度特征识别**
   - 通常 S1 强度 > S2 强度
   - 但允许 S1/S2 强度比小于 0.7 的情况（S2 较强）
   - 配对的两个 onset 都需要有足够强度

3. **验证机制**
   - 检查下一个 onset 的间隔（确认 diastole）
   - 验证配对的时间和强度特征
   - 考虑局部节律的一致性

### 自适应选择策略

#### Auto 模式的自适应算法

1. **评分系统**
   ```typescript
   score = baseStrength 
         + pairingBonus      // 配对加分：+0.3
         + localStrengthBonus // 局部最强加分：+0.1/+0.1
         - s2Penalty          // S2 惩罚：×0.1
   ```

2. **自适应阈值**
   - 基于中位数得分动态调整
   - 考虑灵敏度设置（0.3-1.0）
   - 防止过度或不足检测

3. **间隔验证**
   - 追踪最近 5 个间隔
   - 计算自适应间隔范围
   - 允许 ±50% 的变化

4. **缺口恢复**
   - 追踪连续拒绝次数
   - 12 次连续拒绝后强制接受合理拍点
   - 防止长时间检测中断

### 心率范围应用

```typescript
// 在检测开始时设置范围
const minInterval = 60 / maxBPM;  // 最短拍间隔（秒）
const maxInterval = 60 / minBPM;  // 最长拍间隔（秒）

// 在验证时使用
if (interval >= minInterval * 0.75 && interval <= maxInterval * 1.25) {
  // 接受拍点
}
```

## UI 组件更新

### 新增控件

1. **S1/S2 处理模式选择**
   - 三个按钮：自动 / 仅 S1 / S1-S2 配对
   - 显示当前模式的说明

2. **心率范围滑块**
   - 两个独立滑块：最小和最大
   - 实时显示当前值
   - 提示建议范围

3. **心率范围说明**
   - 显示当前设置的范围
   - 提示缩小范围可提高准确度

### UI 布局

```
心音检测高级选项
├── S1/S2 处理模式
│   └── [自动] [仅 S1] [S1-S2 配对]
├── 心率范围（BPM）
│   ├── 最小：[30━━━━━150] 40
│   └── 最大：[100━━━━━350] 300
├── 检测模式
│   └── [简单] [标准] [精确]
├── 灵敏度：[0.3━━━━━1.0]
├── 滤波频率：[150━━━━━250 Hz]
└── ☑ 启用降噪处理
```

## 使用示例

### 场景 1：正常成人心音（清晰 S1-S2）

```typescript
// 推荐配置
{
  s1s2Mode: 'auto',        // 或 's1-s2-pair'
  minBPM: 50,
  maxBPM: 120,
  mode: 'standard',
  sensitivity: 0.6
}
```

### 场景 2：不清晰的心音（S2 不明显）

```typescript
// 推荐配置
{
  s1s2Mode: 's1-only',     // 只检测 S1
  minBPM: 50,
  maxBPM: 150,
  mode: 'standard',
  sensitivity: 0.7          // 提高灵敏度
}
```

### 场景 3：运动后心音（心率较快）

```typescript
// 推荐配置
{
  s1s2Mode: 'auto',
  minBPM: 100,              // 提高下限
  maxBPM: 200,              // 提高上限
  mode: 'precise',          // 使用精确模式
  sensitivity: 0.6
}
```

### 场景 4：婴儿/儿童心音

```typescript
// 推荐配置
{
  s1s2Mode: 'auto',
  minBPM: 80,               // 婴儿心率较高
  maxBPM: 180,
  mode: 'standard',
  sensitivity: 0.6
}
```

### 场景 5：运动员静息心音

```typescript
// 推荐配置
{
  s1s2Mode: 'auto',
  minBPM: 40,               // 运动员心率较低
  maxBPM: 100,
  mode: 'standard',
  sensitivity: 0.6
}
```

## 优化要点

### 1. 准确性提升
- ✅ 智能区分 S1 和 S2
- ✅ 适应 S2 缺失或强度变化
- ✅ 自适应阈值和间隔验证
- ✅ 可配置心率范围减少误判

### 2. 灵活性提升
- ✅ 三种 S1/S2 处理模式
- ✅ 用户可调心率范围
- ✅ 适应不同类型的心音
- ✅ 多种检测模式选择

### 3. 鲁棒性提升
- ✅ 缺口恢复机制
- ✅ 自适应间隔追踪
- ✅ 绝对最小间隔保护（100ms）
- ✅ 连续拒绝限制防止漏检

### 4. 性能优化
- ✅ 异步处理避免 UI 阻塞
- ✅ 分块处理大文件
- ✅ 可选的降噪处理
- ✅ 进度反馈

## 测试建议

### 测试场景

1. **标准心音**
   - 清晰的 S1-S2 配对
   - 验证三种模式都能工作

2. **S2 缺失**
   - 部分拍点没有 S2
   - Auto 模式应该能正确识别

3. **S2 微弱**
   - S2 强度很低
   - 验证不会误判为独立拍点

4. **心率变化**
   - 心率从慢到快
   - 验证自适应追踪

5. **极端心率**
   - 测试 40 BPM 和 300 BPM
   - 验证范围限制有效

### 预期结果

- **Auto 模式**：适用于大多数情况，准确率高
- **S1-Only 模式**：S2 不清晰时使用，避免误判
- **Pair 模式**：S1-S2 都清晰时使用，最高准确度
- **心率范围**：缩小范围提高准确度，扩大范围提高召回率

## 相关文件

- `frontend/src/utils/bpmDetector.ts` - 核心算法实现
- `frontend/src/components/BPMSettings.vue` - UI 组件
- `frontend/src/stores/audio.ts` - 状态管理

## 版本信息

- **日期**：2026-02-01
- **作者**：AI Assistant
- **版本**：2.0
- **状态**：已实现
